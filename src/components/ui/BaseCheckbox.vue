<template>
  <div class="flex items-center gap-2 mt-2 cursor-pointer" @click="toggleCheckbox">
    <div class="relative w-6 xs:w-5 h-6 xs:h-5 border border-black rounded-sm flex items-center justify-center transition-all duration-300"
         :class="{ 'bg-transparent': modelValue }">
      <svg v-if="modelValue" class="w-4 h-4 stroke-black" viewBox="0 0 24 24" fill="none" stroke="currentColor"
           stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
        <polyline points="20 6 9 17 4 12"></polyline>
      </svg>
    </div>
    
    <label class="text-base xs:text-sm cursor-pointer select-none" :for="id">
      {{ label }}
    </label>

    <input type="checkbox" :id="id" :checked="modelValue" @change="toggleCheckbox" class="hidden" />
  </div>
</template>

<script setup>
import { defineProps, defineEmits } from "vue";

const props = defineProps({
  modelValue: Boolean,
  label: String,
  id: String,
});

const emit = defineEmits(["update:modelValue"]);

const toggleCheckbox = () => {
  emit("update:modelValue", !props.modelValue);
};
</script>
